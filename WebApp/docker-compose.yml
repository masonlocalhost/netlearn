version: "3.9"

services:
  postgres:
    image: postgres:18
    container_name: postgres_db
    restart: always
    environment:
      POSTGRES_PASSWORD: password@1
      POSTGRES_DB: nitro-database
    ports:
      - "5432:5432"
    volumes:
      - ./.data:/var/lib/postgresql
